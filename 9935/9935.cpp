/* 9935 ??4 ??? ??
?? ??? ?? ??? ??? ?? ?? ???? ???? ??? --> ????? ?.
? ????? ??? O(n[?????]) *O(????) ???
??? ?????

*/
#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <queue>
#include <stack>
using namespace std;
string str, bomb;
vector<char> s;
string answer;
int idx = 0;
int main()
{
    cin >> str >> bomb;
    // cout<<str.size()<<endl<<bomb.size();

    for (int i = 0; i < str.length(); i++) //???? ???? ?? ??? ????.
    {

        s.push_back(str[i]);
        //? ???? ???? ??? ??? ???? ??????? ???
        if (s.size() >= bomb.size()) //??? ???? ??? ??? ??.
        {
            bool flag = true;
            /*
            if(s[s.size()-1]==bomb[bomb.size()-1]) //?? ??? ???? ??? ???? ???.
            {
                //cout<<s[s.size()-1]<<endl;
                for(int j=0; j<bomb.size();j++)
                {
                    if(s[s.size()-bomb.size()+j]==bomb[j]) //???? ??? ??? ???? ??? ???? ??? ??? ??? ??? ???.
                    {
                        cout<<"??????? "<<s[s.size()-bomb.size()+j];
                        flag=false;
                        break;
                    }
                    /*?? ? ??
                    {
                        if(s[s.size()-1-bomb.size()+j]==bomb[j])  //????? 4? ?? c? ?????.
                        {
                            flag=false;
                            break;
                        }
                    }
                }
            }
            */
            for (int j = 0; j < bomb.size(); j++)
            {
                if (s[s.size() - bomb.size() + j] != bomb[j])
                {
                    flag = false;
                    break;
                }
            }
            // flag?  true? ??? ??????? ??? ???? ???? ???? ??? ???? ??????.
            if (flag == true)
            { //??? ???? ???? ????? ???.
                for (int k = 0; k < bomb.size(); k++)
                {
                    // cout << "?? ?? ??? " << s[s.size() - 1];
                    s.pop_back();
                }
                // cout << endl;
            }
        }
    }
    if (s.size() == 0)
    {
        cout << "FRULA";
    }
    else
    { //?? ?? ????
        for (int i = 0; i < s.size(); i++)
        {
            cout << s[i];
        }
    }
}
